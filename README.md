# BingeBox

Get up and running:

```bash
# Go to the client folder
cd client

# Start the JSON Server
npx json-server --watch server/database.json --port 4000

# Start the development server (perhaps open another terminal)
npm run dev
